## Magento 2 event bridge to Cloudwatch event

### Getting started

Copy the whole code into `app/code/Bitbull/Mimmo`, install module and then configure it in "Store Configuration" -> "Services" -> "Mimmo Serverless".
